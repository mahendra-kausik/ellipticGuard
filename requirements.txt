# requirements.txt — EllipticGuard
# Python 3.11+ required.
# Install: pip install -r requirements.txt
#
# Pins use >= to stay non-breaking but flexible.
# If you hit a conflict, pin the offending package more tightly and log it in DECISIONS.md.
# GNN dependencies (torch, torch-geometric) are NOT listed here — install them separately
# on Colab when you reach optional Layer 8. They are too heavy for the core CPU environment.

# ── Layer 0: core environment ──────────────────────────────────────────────────
python-dotenv>=1.0.0          # load .env into os.environ

# ── Layers 1–5: data + modelling ──────────────────────────────────────────────
numpy>=1.26.0,<3.0.0
pandas>=2.1.0
scipy>=1.11.0
scikit-learn>=1.4.0
xgboost>=2.0.0
networkx>=3.2.0               # graph construction + topology features
joblib>=1.3.0                 # model serialisation / parallelism (pulled by sklearn, pinned explicitly)
matplotlib>=3.8.0             # EDA plots + per-time-step F1 curve
seaborn>=0.13.0               # EDA

# ── Layer 0+: MLOps — versioning & tracking ────────────────────────────────────
dvc>=3.30.0                   # data & model versioning pipeline
dvc-gdrive>=3.0.0             # Google Drive remote backend for DVC
mlflow>=2.12.0                # experiment tracking + model registry

# ── Layer 9: monitoring ────────────────────────────────────────────────────────
evidently>=0.5.0              # feature-drift + target-drift reports

# nannyml is optional (CBPE label-free performance estimation).
# Uncomment when you reach Layer 9 if you want it:
# nannyml>=0.12.0

# ── Layer 7: serving ──────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0               # request/response schemas (FastAPI v2 uses pydantic v2)
httpx>=0.27.0                 # async HTTP client used by FastAPI TestClient in tests

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0             # coverage reports
